# golangci-lint configuration
# https://golangci-lint.run/usage/configuration/

version: "2"
run:
  tests: true
linters:
  default: none
  enable:
    - dupl
    - errcheck
    - errorlint
    - gocritic
    - gocyclo
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - usetesting
  settings:
    dupl:
      threshold: 200
    errcheck:
      check-type-assertions: false
      check-blank: false
    gocritic:
      enabled-checks:
        - boolExprSimplify
        - emptyStringTest
        - evalOrder
        - nilValReturn
        - regexpPattern
        - truncateCmp
        - unnecessaryBlock
        - weakCond
    gocyclo:
      min-complexity: 55
    revive:
      rules:
        - name: import-shadowing
          severity: warning
          disabled: false
        - name: exported
          severity: warning
          disabled: false
          arguments:
            - "checkPrivateReceivers"
            - "sayRepetitiveInsteadOfStutters"
    unconvert:
      fast-math: false
      safe: false
    unparam:
      check-exported: false
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - gocyclo
        path: _test\.go
      - linters:
          - gocyclo
        path: cmd/ocms/main\.go
      - linters:
          - gocritic
        path: internal/handler/frontend\.go
        text: ifElseChain
      - linters:
          - revive
        text: "exported: type name will be used as|import-shadowing:"
      - linters:
          - staticcheck
        text: 'SA9003:'
      - linters:
          - staticcheck
        text: 'QF1008|QF1012'
      - linters:
          - unparam
        path: _test\.go
    paths:
      - .*\.sql\.go$
      - vendor
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
