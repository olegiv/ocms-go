# oCMS API configuration
# Base URL for the oCMS API (default: /api/v1 which uses the Vite proxy)
VITE_API_URL=/api/v1

# API key for authenticated endpoints (optional for public read-only access)
# Create one at: http://localhost:8080/admin/api-keys
VITE_API_KEY=
