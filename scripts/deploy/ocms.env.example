# Copyright (c) 2025-2026 Oleg Ivanchenko
# SPDX-License-Identifier: GPL-3.0-or-later
#
# oCMS Environment Configuration
# Copy to: /etc/ocms/ocms.env
# Set permissions: chmod 600 /etc/ocms/ocms.env && chown ocms:ocms /etc/ocms/ocms.env

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Session encryption secret (REQUIRED)
# Generate with: openssl rand -base64 32
# Must be at least 32 bytes for AES-256 encryption
OCMS_SESSION_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server bind address
# Use 127.0.0.1 when behind a reverse proxy (recommended)
# Use 0.0.0.0 to bind to all interfaces (not recommended for production)
OCMS_SERVER_HOST=127.0.0.1

# Server port
OCMS_SERVER_PORT=8081

# Environment: development or production
# Production mode enables secure cookies and stricter security
OCMS_ENV=production

# Log level: debug, info, warn, error
OCMS_LOG_LEVEL=warn

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database file path
OCMS_DB_PATH=/var/lib/ocms/data/ocms.db

# =============================================================================
# THEME CONFIGURATION
# =============================================================================

# Directory containing theme files
OCMS_THEMES_DIR=/var/lib/ocms/themes

# Active theme name (can be overridden in admin panel)
OCMS_ACTIVE_THEME=default

# =============================================================================
# CACHING CONFIGURATION (OPTIONAL)
# =============================================================================

# Redis URL for distributed caching (optional)
# Enables cache sharing across multiple oCMS instances
# Format: redis://[user:password@]host:port/database
# OCMS_REDIS_URL=redis://localhost:6379/0

# Redis key prefix (optional)
# OCMS_CACHE_PREFIX=ocms:

# Cache TTL in seconds (optional)
# OCMS_CACHE_TTL=3600

# Maximum in-memory cache entries (optional)
# OCMS_CACHE_MAX_SIZE=10000

# =============================================================================
# SECURITY CONFIGURATION (OPTIONAL)
# =============================================================================

# hCaptcha site key for bot protection on login page
# OCMS_HCAPTCHA_SITE_KEY=your-site-key

# hCaptcha secret key
# OCMS_HCAPTCHA_SECRET_KEY=your-secret-key

# API rate limit (requests per minute per API key)
# OCMS_API_RATE_LIMIT=100
