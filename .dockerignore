# Copyright (c) 2025-2026 Oleg Ivanchenko
# SPDX-License-Identifier: GPL-3.0-or-later

# Docker build context exclusions
# Keep the build context small for faster builds

# Git
.git
.gitignore
.gitattributes

# Build outputs
bin/
data/
uploads/

# Node modules (will be installed fresh in container)
node_modules/

# Compiled assets (will be built in container)
web/static/dist/

# Environment files (secrets should be passed via env vars)
.env
.env.*
!.env.example

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Claude Code settings
.claude/
cookies.txt

# Documentation (not needed at runtime)
docs/
*.md
!README.md
LICENSE

# Audit files
.audit/

# Test files
*_test.go
testdata/

# Log files
*.log

# Docker files (avoid recursive builds)
Dockerfile*
docker-compose*.yml
.dockerignore

# Custom themes (loaded from filesystem at runtime)
custom/themes/*
!custom/themes/.gitkeep

# Custom modules are compiled into the binary, so they
# must remain in the build context (not excluded here).

# Development scripts
scripts/
Makefile
